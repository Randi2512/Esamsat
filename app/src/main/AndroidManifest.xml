<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.esamsat">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.GIS.AppController"
        android:allowBackup="true"
        android:icon="@drawable/logo_apk"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.MONITOR.Monitoring"></activity>
        <activity android:name=".info.SamKel.PERIODE.Periode" />
        <activity android:name=".ui.SamkelToday.SamkelToday" />
        <activity android:name=".info.DataCekPajak" />
        <activity android:name=".TentangAplikasi.Panduan" />
        <activity android:name=".TentangAplikasi.LayananSamsat" />
        <activity
            android:name=".info.Peraturan.PeraturanpajakActivity"
            android:label="Peraturan Pajak" />
        <activity
            android:name=".info.Pembayaran.PembayaranActivity"
            android:label="Cara Pembayaran" />
        <activity android:name=".ui.InfoActivity" />
        <activity android:name=".info.Peraturan.PdfPeraturan" />
        <activity android:name=".info.Peraturan.DetailPeraturan" />
        <activity android:name=".info.Peraturan.PeraturanPajak" />
        <activity android:name=".info.PengaduanMasyarakat" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".ui.GIS.MapsActivity"
            android:label="GIS PAYMENT POINT" />
        <activity
            android:name=".info.faq.faq"
            android:label="Layanan FAQ" />
        <activity
            android:name=".ui.home.HomeActivity"
            android:label="Home" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".info.SamKel.lokasi_samkel"
            android:label="Lokasi Samsat Keliling" />
        <activity
            android:name=".info.Pengaduan"
            android:label="Pengaduan" />
        <activity
            android:name=".info.SamKel.UPTD.SamsatKeliling"
            android:label="Samsat Keliling" />
        <activity
            android:name=".info.CekPajakActivity"
            android:label="Cek Pajak Kendraan" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>